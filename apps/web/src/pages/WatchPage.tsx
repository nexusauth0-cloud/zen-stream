import { PlaceholderPage } from "../components/PlaceholderPage/PlaceholderPage";

export function WatchPage() {
  return (
    <PlaceholderPage
      title="Watch"
      description="The player arrives in a later milestone."
      route="/watch/:subjectId"
    />
  );
}